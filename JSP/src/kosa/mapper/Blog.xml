<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="kosa.mapper.BlogMapper">
	<cache />
	<select id="selectBlog" parameterType="int" resultType="">
		select b.id, b.title, b.content, a.author_id, a.name, a.address
		from blog b, author a where
		b.author_id = a.author_id and id=${id}
	</select>	
</mapper>
